export class OverflowWatcher { static _observed=new WeakMap(); static enable(root,selector="[data-cxp-scroll]"){if(!root?.querySelectorAll)return;for(const el of root.querySelectorAll(selector))this.watch(el);const wc=root.querySelector?.(".window-content");if(wc)this.watch(wc)} static watch(el){if(!el||this._observed.has(el))return;const o=new ResizeObserver(()=>this.evaluate(el));o.observe(el);this._observed.set(el,o);this.evaluate(el)} static evaluate(el){el.classList.toggle("cxp-overflow-x",el.scrollWidth>el.clientWidth+1);el.classList.toggle("cxp-overflow-y",el.scrollHeight>el.clientHeight+1)} }
